# Main Docker Compose file for OpenTelemetry Course
# This file includes the observability stack and application

include:
  - compose.observability.yaml
  - compose.app.yaml

# TODO(absurek): Hotfix for DooD DevContainer relative paths
# remove absulute paths

services:
  frontend:
    build:
      context: ../app/frontend
      dockerfile: Dockerfile.dev
      target: development
    volumes:
      # Mount source code for hot reload
      - /home/sureka/github.com/absurek/udemy-opentelemetry-for-observability/app/frontend/src:/app/src:ro
      - /home/sureka/github.com/absurek/udemy-opentelemetry-for-observability/app/frontend/tsconfig.json:/app/tsconfig.json:ro
    environment:
      - NODE_ENV=development
      - LOG_LEVEL=debug
    command: npm run dev

  worker:
    build:
      context: ../app/worker
      dockerfile: Dockerfile.dev
    volumes:
      # Mount source code for hot reload
      - /home/sureka/github.com/absurek/udemy-opentelemetry-for-observability/app/worker/src:/app/src:ro
      # Preserve translation models (large, slow to download)
      - worker-models:/root/.local/share/argos-translate
    environment:
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=debug
    command: watchmedo auto-restart --directory=./src --pattern="*.py" --recursive -- python -m src.main

volumes:
  worker-models:

# Shared networks for all services
networks:
  observability:
    driver: bridge
    name: observability
  app:
    driver: bridge
    name: app
